pipline_test:
  stage: .pre
  script:
    - echo "Pipline Works!!"

build:
  stage: build
  script:
    - apt-get update && apt-get install make
    - make
  artifacts:
    paths:
      - hello
    expire_in: 1 week